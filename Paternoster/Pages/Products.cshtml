@page
@using Paternoster.Models
@model ProductsModel
@{
    ViewData["Title"] = "Producten";
}
<h1>Zoek een product:</h1>
<div class="container">
    <form method="get">
        <p>Zoek een Product:</p>
        <label for="Name"> Naam van het Product:</label>
        <input name="Name" />
        <input type="submit" value="&#x1F50D;" />
    </form>
</div>

<div class="container">
    @if (Model.Products == null)
    {
        <h2>Sorry, maar ik heb geen roducten gevonden. Probeer een nieuwe zoekopdracht.</h2>
    }
    else
    {
        foreach (Product product in Model.Products)
        {
            <div class="container">
                <h3>Naam:</h3>
                <p>@product.Name</p>
                <h3>Code:</h3>
                <p>@product.ProductCode</p>
                <h3>Onderdelen</h3>
                <ul>
                    @foreach(ProductPart part in product.ProductParts)
                    {
                        <li>@part.PartAmount x @part.Part.Name</li>
                    }
                </ul>
            </div>
        }
    }
</div>

